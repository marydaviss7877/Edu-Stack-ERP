# Dependencies
node_modules/
backend/node_modules/
frontend/node_modules/

# Build outputs
backend/dist/
frontend/dist/

# Environment files
.env
backend/.env
frontend/.env
*.env.local

# Logs
*.log
npm-debug.log*

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/settings.json
.idea/

# Coverage
coverage/
backend/coverage/

# Local dev artifacts
CREDENTIALS.md
EduStack_Upgraded_Brochure.html
scratch/

# Google service account keys (NEVER commit these)
*service-account*.json
*serviceaccount*.json
edu-stack-505509-*.json

# Certificates (NEVER commit these)
certificates/
certificates.zip
deployment_cert.der
*.der

# Database backups / data exports (contain real student, staff, and financial records — NEVER commit these)
/backups/
backend/backups/
economic-transactions-summary.xlsx

# Headless Chrome profile used by store-assets rendering (cache/cookies, not a deliverable)
mobile/store-assets/.chrome-render-profile/
